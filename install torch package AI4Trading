1. Check cuda version
nvcc --version   
If Command 'nvcc' not found, but can be installed with:
sudo apt install nvidia-cuda-toolkit

2. Use this URL: https://pytorch.org/ to find install torch command
pip3 install --user torch torchvision

3. Missed matplotlib
pip3 install --user matplotlib

